import { HighlightwarningDirective } from './highlightwarning.directive';

describe('HighlightwarningDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightwarningDirective();
    expect(directive).toBeTruthy();
  });
});
